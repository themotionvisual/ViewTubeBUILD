# ViewTube — Complete Node Modules Reference

**Total packages:** ~370 | **Project:** ViewTube | **Generated:** April 2026

---

## 🔵 Scoped Packages (`@namespace/...`)

1. **@babel** — Collection of Babel compiler tools that transforms modern JavaScript/TypeScript into browser-compatible code during the build process
2. **@emnapi** — N-API emulation layer for WebAssembly that allows native Node.js addons to run in browser environments (used by ONNX Runtime Web)
3. **@eslint** — Core ESLint packages including the config system and rules that provide the linting engine enforcing code quality
4. **@eslint-community** — Community-maintained ESLint utilities providing shared rule helpers and plugins used across multiple ESLint plugins
5. **@google** — Google API client libraries providing authenticated access to Google APIs including YouTube Analytics and Data API
6. **@humanfs** — Cross-platform filesystem abstraction providing a consistent file system interface that works across Node environments (used by ESLint)
7. **@humanwhocodes** — Utility packages by Nicholas Zakas (ESLint creator) including config array logic and other ESLint internals
8. **@imgly** — IMG.LY image processing libraries providing advanced image manipulation capabilities
9. **@jridgewell** — Source map utilities by Justin Ridgewell used during build to generate accurate source maps for debugging minified code
10. **@napi-rs** — Rust-based native Node.js addon framework enabling high-performance native bindings used by tools like LightningCSS
11. **@oxc-project** — Oxidation Compiler project tools providing ultra-fast JavaScript/TypeScript parsing and transformation (used in Vite build)
12. **@protobufjs** — Protocol Buffers for JavaScript scoped packages handling binary serialization for Google API communication
13. **@puppeteer** — Puppeteer scoped sub-packages providing browser automation helpers used for testing and screenshot generation
14. **@reduxjs** — Redux Toolkit scoped packages providing the official opinionated Redux toolset with simplified store setup and RTK Query
15. **@rolldown** — Rolldown bundler scoped packages providing the Rust-based JavaScript bundler that is Vite's next-gen bundler
16. **@standard-schema** — Standard schema specification types providing a shared interface for schema validation libraries (Zod compatibility)
17. **@tailwindcss** — Tailwind CSS scoped plugins and tools including the Vite plugin and PostCSS integration for Tailwind v4
18. **@tootallnate** — HTTP/HTTPS proxy utilities providing proxy agent support for Node.js HTTP requests (used by Puppeteer)
19. **@tybys** — WASM-related binding tools providing low-level WebAssembly utilities used by ONNX Runtime
20. **@types** — TypeScript type definitions providing `.d.ts` type declarations for packages that don't ship their own (React, Node, etc.)
21. **@typescript-eslint** — TypeScript-aware ESLint rules enabling ESLint to understand and lint TypeScript-specific syntax and patterns
22. **@ungap** — Polyfills for older environments providing missing browser API implementations (WeakMap, structuredClone, etc.)
23. **@vitejs** — Vite scoped plugins including `@vitejs/plugin-react` which enables Fast Refresh and JSX transformation in Vite

---

## 🟢 A

24. **acorn** — Fast, tiny JavaScript parser that parses JS source code into an AST; used by ESLint and other build tools
25. **acorn-jsx** — JSX syntax extension for Acorn that adds JSX parsing support so ESLint can lint React files
26. **agent-base** — Base class for HTTP/HTTPS agent creation providing the foundation for custom proxy agents in Node.js
27. **ajv** — Another JSON Validator that validates JSON data against JSON Schema; used by ESLint to validate config files
28. **ansi-regex** — Regular expression that matches ANSI escape codes used to detect color/formatting control sequences in terminal strings
29. **ansi-styles** — ANSI color and style codes for terminal output providing the raw ANSI codes used by Chalk for colorizing CLI output
30. **argparse** — Command-line argument parser (Python argparse port) that parses CLI arguments for tools like js-yaml
31. **ast-types** — JavaScript AST type definitions providing type-safe representations of AST nodes used by code transformation tools
32. **autoprefixer** — PostCSS plugin that automatically adds `-webkit-`, `-moz-`, etc. vendor prefixes for browser compatibility

---

## 🟢 B

33. **b4a** — Buffer-to-ArrayBuffer conversion utility that converts between Node.js Buffers and Web API ArrayBuffers seamlessly
34. **bail** — Tiny utility that throws an error if passed one; used in unified/remark processing pipelines
35. **balanced-match** — Matches balanced pairs like `{ }` or `[ ]`; used by brace-expansion to parse glob patterns
36. **bare-events** — Lightweight EventEmitter for the Bare JavaScript runtime environment
37. **bare-fs** — Filesystem module providing file system access in the Bare runtime (Electron alternative)
38. **bare-os** — OS utilities providing OS-level information in the Bare runtime environment
39. **bare-path** — Cross-platform path utilities for the Bare runtime
40. **bare-stream** — Provides readable/writable stream implementations for the Bare runtime
41. **bare-url** — Provides URL parsing and construction for the Bare runtime
42. **base64-js** — Base64 encoding/decoding in pure JavaScript; converts binary data to/from base64 strings
43. **baseline-browser-mapping** — Maps browser names to baseline versions for CSS feature targeting
44. **basic-ftp** — FTP client for Node.js providing file transfer capabilities (used by Puppeteer for downloading Chromium)
45. **bignumber.js** — Arbitrary-precision decimal arithmetic that handles very large or precise numbers beyond JavaScript's float limits
46. **brace-expansion** — Bash-style brace expansion that expands `{a,b}` patterns in glob strings like `*.{ts,tsx}`
47. **browserslist** — Queries the list of target browsers from `.browserslistrc` to determine browser support for PostCSS/Babel
48. **buffer-crc32** — Computes CRC-32 checksum values over Buffers for data integrity verification
49. **buffer-equal-constant-time** — Compares two buffers without timing attacks; used in JWT/auth libraries

---

## 🟢 C

50. **callsites** — Inspects V8 call sites in the current call stack and returns stack frame information for debugging
51. **caniuse-lite** — Lightweight version of Can I Use browser data providing compatibility info for Autoprefixer and Browserslist
52. **ccount** — Counts specific characters in a string; used in Markdown processing pipelines
53. **chalk** — Terminal string styling library that colorizes and styles console output for readable CLI messages
54. **character-entities** — HTML character entity definitions providing mappings from entity names to Unicode characters (e.g. `&amp;` → `&`)
55. **character-entities-html4** — The older HTML4 subset of character entities for legacy compatibility
56. **character-entities-legacy** — Character entity names that were supported before HTML5
57. **character-reference-invalid** — Identifies character references that are not valid according to the HTML spec
58. **chromium-bidi** — Chrome DevTools BiDi (bidirectional) protocol enabling two-way browser automation communication for Puppeteer
59. **cliui** — Formats command-line help text into aligned columns (used by yargs)
60. **clsx** — Tiny utility for conditionally joining CSS class names together; useful with Tailwind
61. **color-convert** — Converts colors between RGB, HSL, HEX, and other color formats
62. **color-name** — Maps CSS color names like `"red"` to RGB values like `[255, 0, 0]`
63. **comma-separated-tokens** — Parses and stringifies comma-separated HTML attribute values in AST processing
64. **concat-map** — Applies a function that returns arrays over an input and concatenates the results
65. **convert-source-map** — Converts source maps between inline, file, and data URI formats during build
66. **cookie** — Parses `Cookie` headers and serializes `Set-Cookie` values for HTTP requests
67. **core-util-is** — Provides `isArray`, `isString`, etc. type-checking functions for older Node.js compat
68. **cosmiconfig** — Universal configuration file loader that finds config from package.json, `.rc` files, etc. (used by Tailwind)
69. **cross-spawn** — Fixes Windows compatibility issues when spawning child processes
70. **csstype** — TypeScript type definitions for all CSS property names and values

---

## 🟢 D

71. **d3-array** — Statistical functions, bisect, histogram, and sorting for data arrays; used by Recharts
72. **d3-color** — Parses, converts, and manipulates colors in RGB, HSL, Lab color spaces; used by Recharts
73. **d3-ease** — Provides timing functions for smooth animations (ease-in, ease-out, bounce, etc.); used by Recharts
74. **d3-format** — Formats numbers with thousands separators, fixed decimals, SI prefixes; used by Recharts axes
75. **d3-interpolate** — Smoothly transitions between two values (numbers, colors, strings); used by Recharts animations
76. **d3-path** — Generates SVG path `d` attribute strings for custom shapes; used by Recharts
77. **d3-scale** — Maps data values to visual properties (linear, log, ordinal scales); used by Recharts
78. **d3-shape** — Draws lines, areas, arcs, and curves from data arrays; core to Recharts chart drawing
79. **d3-time** — Provides time-based intervals (day, month, year) for time-series charts
80. **d3-time-format** — Formats and parses dates using strftime-style format strings for chart axes
81. **d3-timer** — Manages animation frames and timeouts with high-precision scheduling; used by Recharts transitions
82. **data-uri-to-buffer** — Parses `data:` URI strings into binary Buffer objects
83. **debug** — Creates namespaced debug loggers that can be enabled via the `DEBUG` env variable
84. **decimal.js-light** — Lightweight arbitrary-precision decimal library that handles precise math without floating point errors
85. **decode-named-character-reference** — Converts `&amp;`, `&lt;`, etc. HTML named references to their actual characters
86. **deep-is** — Deep equality check that correctly handles `NaN === NaN` (unlike standard deepEqual)
87. **degenerator** — Transforms regular functions into async generators for proxy resolution
88. **dequal** — Checks if two values are deeply equal with support for all JavaScript types
89. **detect-libc** — Identifies glibc vs musl for selecting the correct native binary on Linux systems
90. **devlop** — Provides `ok()` assertions that are stripped out in production builds
91. **devtools-protocol** — TypeScript type definitions for all Chrome DevTools Protocol commands and events (used by Puppeteer)

---

## 🟢 E

92. **ecdsa-sig-formatter** — Converts between DER and raw ECDSA signatures for JWT verification
93. **electron-to-chromium** — Maps Electron versions to Chromium versions; used by Browserslist to resolve Electron browser targets
94. **emoji-regex** — Detects emoji characters in strings for accurate string width calculation
95. **end-of-stream** — Fires a callback when a readable/writable/duplex stream is fully finished
96. **enhanced-resolve** — Webpack's module resolution engine that resolves `import` paths according to Node.js rules
97. **env-paths** — Returns OS-appropriate directories for data, config, cache, logs, and temp files
98. **error-ex** — Extends native Error with named error types and extra metadata for custom error types
99. **es-toolkit** — Modern JavaScript utility library providing `chunk`, `debounce`, `groupBy` with better TypeScript support
100. **escalade** — Traverses parent directories to find config files or package roots
101. **escape-string-regexp** — Makes arbitrary strings safe to use inside regular expressions by escaping special characters
102. **escodegen** — Converts an ESTree AST back into JavaScript source code string
103. **eslint** — Analyzes source code for potential errors, style violations, and best practices using configurable rules
104. **eslint-plugin-react-hooks** — Enforces the Rules of Hooks (no conditional hooks, proper deps arrays) via ESLint
105. **eslint-plugin-react-refresh** — Warns when components won't hot-reload correctly in Vite's Fast Refresh
106. **eslint-scope** — Tracks variable declarations, references, and scopes for ESLint rule analysis
107. **eslint-visitor-keys** — Tells ESLint which properties of each AST node contain child nodes to visit
108. **espree** — Wraps Acorn with ESLint-specific extensions to parse modern JavaScript
109. **esprima** — Classic JavaScript parser that parses ECMAScript into an AST; used by escodegen
110. **esquery** — Queries AST nodes using CSS-selector-like syntax
111. **esrecurse** — Traverses ESTree AST nodes recursively; used by ESLint's scope analysis
112. **estraverse** — Provides `traverse()` and `replace()` for walking ESTree AST node trees
113. **estree-util-is-identifier-name** — Validates that a string can be used as a JavaScript variable name
114. **esutils** — Provides code generation helpers and AST utility functions for ESTree manipulation
115. **eventemitter3** — Faster alternative to Node's built-in EventEmitter; used by several libraries
116. **events-universal** — Provides Node.js-style EventEmitter that works in both Node and browser environments
117. **extend** — Shallow/deep copies properties from source objects to a target object
118. **extract-zip** — Unzips `.zip` files to a directory; used by Puppeteer to unpack Chromium downloads

---

## 🟢 F

119. **fast-deep-equal** — The fastest deep-equal implementation; used by AJV for JSON schema comparison
120. **fast-fifo** — Efficient first-in-first-out queue for stream buffering
121. **fast-json-stable-stringify** — Serializes objects with keys always in the same order for deterministic output
122. **fast-levenshtein** — Computes string edit distance; used by ESLint for "did you mean?" suggestions
123. **fd-slicer** — Reads specific byte ranges from files without race conditions; used in ZIP extraction
124. **fdir** — Lists files recursively at very high speed; used internally by Vite for file discovery
125. **fetch-blob** — Provides the browser `Blob` class in Node.js environments
126. **file-entry-cache** — Stores file modification times to skip linting unchanged files between runs
127. **find-up** — Searches parent directories until it finds the target file or reaches the filesystem root
128. **flat-cache** — Persists data to JSON files on disk; used by ESLint to cache lint results between runs
129. **flatbuffers** — Google's zero-copy binary serialization format; used by ONNX Runtime for model data
130. **flatted** — Safely stringifies objects with circular references; used by flat-cache
131. **formdata-polyfill** — Provides the browser `FormData` API in Node.js server environments
132. **fraction.js** — Handles exact rational number arithmetic; used by Tailwind for precise CSS calculations
133. **framer-motion** — Production-ready React animation library providing declarative animations, gestures, layout transitions, and spring physics
134. **fsevents** — Provides fast native file change notifications on macOS for Vite's dev server HMR

---

## 🟢 G

135. **gaxios** — Google's fetch-based HTTP client with retry logic; used by google-auth-library for API calls
136. **gcp-metadata** — Retrieves GCP VM instance metadata for service account detection in cloud environments
137. **gensync** — Converts async/generator-based code to run synchronously when needed (used by Babel)
138. **get-caller-file** — Inspects the call stack to determine which file called a function
139. **get-stream** — Reads an entire Node.js stream into memory as a string or Buffer
140. **get-uri** — Gets a ReadableStream from a URI supporting `file:`, `http:`, `https:`, `data:` uniformly
141. **glob-parent** — Extracts the non-magic base path from a glob string
142. **globals** — Lists all built-in globals for browser, Node.js, ES2015+ environments for ESLint
143. **google-auth-library** — Handles OAuth 2.0, service accounts, and API key auth for all Google APIs
144. **google-logging-utils** — Structured logging helpers for Google Cloud services
145. **graceful-fs** — Drop-in replacement for Node `fs` that adds retry logic and fixes edge cases
146. **guid-typescript** — Creates and validates RFC 4122 compliant UUIDs in TypeScript

---

## 🟢 H

147. **has-flag** — Detects if a specific CLI flag exists in `process.argv` (e.g. `--color`)
148. **hast-util-to-jsx-runtime** — Transforms HTML AST nodes into React JSX elements for react-markdown rendering
149. **hast-util-whitespace** — Identifies whitespace-only text nodes in HTML ASTs
150. **hermes-estree** — TypeScript types for the AST produced by Meta's Hermes JavaScript engine
151. **hermes-parser** — Meta's fast Hermes JavaScript parser used in React Native tooling
152. **html-url-attributes** — Lists which HTML attributes (like `href`, `src`) contain URL values
153. **http-proxy-agent** — Routes HTTP requests through an HTTP proxy server
154. **https-proxy-agent** — Routes HTTPS requests through an HTTP CONNECT tunnel proxy

---

## 🟢 I

155. **ignore** — Parses `.gitignore` patterns and filters file lists accordingly (used by ESLint)
156. **immediate** — Provides `setImmediate` in environments that lack it (older browsers polyfill)
157. **immer** — Lets you write mutations on a draft state and produces a new immutable state tree; used by Redux Toolkit
158. **import-fresh** — Re-requires a module fresh every time bypassing the require cache; useful for config reloading
159. **imurmurhash** — Fast non-cryptographic MurmurHash function; used by ESLint file caching
160. **inherits** — Provides prototype-based inheritance for older code compatibility (browser-compatible `util.inherits`)
161. **inline-style-parser** — Converts `style="color: red"` strings into property/value objects
162. **internmap** — D3's `InternMap` and `InternSet` that convert keys by value (handles Date keys, etc.); used by Recharts
163. **iota-array** — Generates `[0, 1, 2, ...]` sequential arrays used in ndarray indexing
164. **ip-address** — Validates, parses, and converts IPv4 and IPv6 addresses
165. **is-alphabetical** — Single-character utility that checks if a character is alphabetical; used in Markdown tokenizers
166. **is-alphanumerical** — Single-character test checking if a character is alphanumeric; used in Micromark parsing
167. **is-arrayish** — Detects array-like objects with `length` properties
168. **is-buffer** — Type guard that returns true only for actual Node.js Buffer instances
169. **is-decimal** — Single-character utility that checks if a character is a decimal digit
170. **is-extglob** — Detects `+(pattern)` and similar extended glob syntax in strings
171. **is-fullwidth-code-point** — Identifies wide CJK characters for accurate terminal width calculation
172. **is-glob** — Detects whether a string contains glob special characters
173. **is-hexadecimal** — Utility for detecting hex digits in low-level parsers
174. **is-plain-obj** — Returns true only for `{}`-style plain objects, not class instances
175. **isarray** — Polyfill for `Array.isArray` for older environments
176. **isexe** — Tests whether a file can be executed; used by `which` for command lookup

---

## 🟢 J

177. **jiti** — Lets tools like Tailwind require `.ts` config files without pre-compilation
178. **js-tokens** — Splits JS source code into tokens without a full parse; used by loose-envify
179. **js-yaml** — Reads and writes YAML files; used by ESLint for reading config files
180. **jsesc** — Safely converts arbitrary strings to JS-compatible escaped string literals
181. **json-bigint** — Handles JSON with integers larger than `Number.MAX_SAFE_INTEGER`
182. **json-buffer** — Serializes/deserializes Node.js Buffers through JSON for keyv storage
183. **json-parse-even-better-errors** — Provides more descriptive errors when JSON parsing fails
184. **json-schema-traverse** — Walks through a JSON Schema visiting all nested schemas; used by AJV
185. **json-stable-stringify-without-jsonify** — Produces stable JSON serialization without the `jsonify` dependency
186. **json5** — Parses JSON5 format which allows comments, trailing commas, and unquoted keys
187. **jszip** — Pure JavaScript ZIP compression/decompression for creating and reading ZIP archives
188. **jwa** — Implements HMAC, RSA, and ECDSA algorithms for JWT signing/verification
189. **jws** — Creates and verifies JWS tokens; used by google-auth-library for API auth

---

## 🟢 K–L

190. **keyv** — Provides a unified key-value store interface with multiple storage backends
191. **levn** — Parses typed string values like `"[1, 2, 3]"` into actual JavaScript values
192. **lie** — Minimal A+ Promises implementation for environments without native Promise support
193. **lightningcss** — Rust-based CSS parser and transformer; used by Vite for extremely fast CSS processing
194. **lightningcss-darwin-arm64** — The native ARM64 binary for LightningCSS on M1/M2/M3 Macs
195. **lines-and-columns** — Converts string character offsets to line/column numbers for error reporting
196. **locate-path** — Checks multiple paths and returns the first one that exists
197. **lodash-es** — The ESM version of Lodash providing utility functions compatible with tree-shaking
198. **lodash.merge** — Recursively merges source objects into a destination object
199. **long** — Provides `Long` type for working with 64-bit integers in JavaScript (used by protobufjs)
200. **longest-streak** — Returns the longest sequence of identical characters in a string
201. **loose-envify** — Replaces `process.env.NODE_ENV` in bundled code based on js-tokens tokenization
202. **lru-cache** — Caches values with automatic eviction of least recently used items when the cache is full
203. **lucide-react** — Provides 1000+ consistent SVG icons as React components; used throughout the ViewTube UI

---

## 🟢 M

204. **magic-string** — Makes surgical string edits while generating correct source maps; used by Vite/Rolldown
205. **mdast-util-from-markdown** — Converts Markdown text to an Abstract Syntax Tree; core of react-markdown
206. **mdast-util-mdx-expression** — Handles `{expression}` syntax in MDX files
207. **mdast-util-mdx-jsx** — Handles `<Component />` syntax in MDX documents
208. **mdast-util-mdxjs-esm** — Handles `import`/`export` statements in MDX files
209. **mdast-util-phrasing** — Determines if an MDAST node is inline (phrasing) vs block level
210. **mdast-util-to-hast** — Transforms Markdown AST to HTML AST for rendering; used by remark-rehype
211. **mdast-util-to-markdown** — Converts a Markdown AST back into Markdown text
212. **mdast-util-to-string** — Extracts plain text content from Markdown AST nodes
213. **micromark** — Byte-level compliant CommonMark parser; the core tokenizer for remark
214. **micromark-core-commonmark** — Adds standard Markdown syntax (headings, lists, code) to Micromark
215. **micromark-factory-destination** — Tokenizes link destinations in Markdown (`[text](url)`)
216. **micromark-factory-label** — Tokenizes link/image labels in reference-style Markdown links
217. **micromark-factory-space** — Handles optional and required spaces in Markdown constructs
218. **micromark-factory-title** — Tokenizes optional titles in link and image definitions
219. **micromark-factory-whitespace** — Creates whitespace-handling tokenizers for Markdown parsing
220. **micromark-util-character** — Provides `asciiAlpha`, `markdownSpace`, etc. character classification tests
221. **micromark-util-chunked** — Efficiently manages the tokenizer's character chunk arrays
222. **micromark-util-classify-character** — Determines if characters are whitespace, punctuation, or other
223. **micromark-util-combine-extensions** — Merges multiple syntax extensions into a single Micromark config
224. **micromark-util-decode-numeric-character-reference** — Converts `&#8203;` style references to Unicode characters
225. **micromark-util-decode-string** — Handles escape sequences and character references in Markdown strings
226. **micromark-util-encode** — Escapes special HTML characters in Micromark output
227. **micromark-util-html-tag-name** — Checks if a string is a valid HTML tag for raw HTML pass-through
228. **micromark-util-normalize-identifier** — Lowercases and collapses whitespace in reference link labels
229. **micromark-util-resolve-all** — Post-processes tokenizer events to build the final token stream
230. **micromark-util-sanitize-uri** — Validates and encodes URIs to prevent XSS in link/image URLs
231. **micromark-util-subtokenize** — Handles nested tokenization within already-tokenized content
232. **micromark-util-symbol** — Defines internal Symbol constants used across Micromark packages
233. **micromark-util-types** — Provides all TypeScript type definitions for Micromark's token types and events
234. **minimatch** — Matches file paths against glob patterns like `**/*.ts`; used by ESLint and other tools
235. **mitt** — Minimalist 200-byte pub/sub event bus for app-wide events with no overhead
236. **motion-dom** — The low-level DOM animation engine that powers Framer Motion
237. **motion-utils** — Shared math and utility functions for the motion animation library
238. **ms** — Converts time strings like `"2 days"` to milliseconds and back; used by debug

---

## 🟢 N

239. **nanoid** — Generates URL-safe unique IDs; used by Vite for cache-busting and chunk naming
240. **natural-compare** — Sorts strings like humans expect (`"9" < "10"` instead of lexicographic)
241. **ndarray** — Provides NumPy-style multidimensional array operations for JavaScript
242. **netmask** — Parses and checks CIDR network addresses; used in proxy resolution
243. **node-domexception** — Provides the browser `DOMException` class in Node.js environments
244. **node-fetch** — Provides the browser `fetch()` API in Node.js environments
245. **node-releases** — Provides Node.js LTS and release dates for Browserslist's Node.js targeting

---

## 🟢 O

246. **object-assign** — Polyfill for `Object.assign` that copies properties from sources to a target object
247. **once** — Wraps a function so repeated calls do nothing after the first invocation
248. **onnxruntime-common** — Shared types and utilities for running ONNX ML models
249. **onnxruntime-web** — Runs ONNX machine learning models in browsers via WebAssembly
250. **optionator** — Parses command-line options with type checking and help generation; used by ESLint

---

## 🟢 P

251. **p-limit** — Runs promises with a maximum concurrency limit to avoid overwhelming APIs
252. **p-locate** — Tries multiple paths concurrently and returns the first that exists
253. **p-retry** — Automatically retries failed promises with exponential backoff
254. **pac-proxy-agent** — Resolves proxy settings from Proxy Auto-Config (PAC) scripts
255. **pac-resolver** — Executes `FindProxyForURL()` from PAC files to determine proxy settings
256. **pako** — Deflate/inflate (gzip, zlib) compression entirely in pure JavaScript
257. **parent-module** — Inspects the call stack to find which module required the current one
258. **parse-entities** — Converts HTML entities like `&amp;` `&lt;` to their actual characters
259. **parse-json** — Wraps `JSON.parse` with more descriptive error messages and position info
260. **path-exists** — Async/sync checks if a file or directory exists at a given path
261. **path-key** — Returns `"PATH"` on Unix and `"Path"` on Windows for env variable access
262. **pend** — Runs tasks in parallel with an optional concurrency limit and fires a callback when done
263. **picocolors** — The fastest terminal color library with zero dependencies (alternative to Chalk)
264. **picomatch** — Fast, reliable glob and micromatch implementation for file path matching
265. **platform** — Detects OS, browser, and runtime environment from user agent strings
266. **postcss** — Processes CSS through a chain of plugins (Autoprefixer, Tailwind, etc.)
267. **postcss-value-parser** — Tokenizes CSS property values like `calc(100% - 20px)` into AST nodes
268. **prelude-ls** — Haskell-inspired functional programming utility functions used by type-check and levn
269. **process-nextick-args** — Polyfill that adds argument passing to `process.nextTick`
270. **progress** — Displays progress bars in the terminal; used by Puppeteer during Chromium download
271. **prop-types** — Runtime type checking for React component props (legacy, mostly replaced by TypeScript)
272. **property-information** — Maps HTML/SVG attribute names to DOM property names and types
273. **protobufjs** — Serializes and deserializes binary data using `.proto` schemas for Google API communication
274. **proxy-agent** — Auto-detects and routes through HTTP, HTTPS, SOCKS4, SOCKS5 proxy servers
275. **proxy-from-env** — Reads `HTTP_PROXY`, `HTTPS_PROXY`, `NO_PROXY` environment variables
276. **pump** — Chains Node.js streams together and properly handles errors and cleanup
277. **punycode** — Converts Unicode domain names to ASCII-compatible encoding for internationalized domains (IDN)
278. **puppeteer** — Controls Chromium for web scraping, testing, and screenshot generation
279. **puppeteer-core** — Same as Puppeteer but requires you to bring your own browser binary (no bundled Chromium)

---

## 🟢 R

280. **react** — Provides `createElement`, hooks, and component lifecycle for building UI components
281. **react-dom** — Renders React components to the browser DOM and handles synthetic events
282. **react-draggable** — Makes elements draggable with mouse/touch support for draggable panel UIs
283. **react-google-charts** — Wraps Google Charts API in React-friendly component interfaces
284. **react-is** — Checks if something is a React element, portal, fragment, etc.
285. **react-markdown** — Safely converts Markdown text to React JSX with plugin support
286. **react-redux** — Provides `useSelector`, `useDispatch`, and `Provider` for connecting React to Redux store
287. **react-resizable** — Adds drag-to-resize behavior for panels and containers
288. **react-router** — Provides `Route`, `Switch`, and navigation primitives for single-page apps
289. **react-router-dom** — Adds browser-specific `BrowserRouter`, `Link`, and `useNavigate` components
290. **readable-stream** — Provides consistent stream implementation across Node.js versions
291. **recharts** — Composable chart library built on D3 and React; renders all ViewTube analytics charts (LineChart, BarChart, PieChart, etc.)
292. **redux** — Manages application state with a single store, pure reducers, and dispatched actions
293. **redux-thunk** — Allows dispatching async functions (thunks) instead of plain action objects
294. **remark-parse** — The Markdown-to-AST plugin for the unified remark processor pipeline
295. **remark-rehype** — Bridges the remark (Markdown) and rehype (HTML) unified ecosystems
296. **require-directory** — Recursively requires all modules in a directory (used by yargs)
297. **reselect** — Creates memoized Redux selector functions that recompute only when dependencies change
298. **resolve-from** — Like `require.resolve` but resolves a module from a specified path
299. **retry** — Retries failed async operations with customizable backoff strategies
300. **rolldown** — Next-generation Vite bundler written in Rust for dramatically faster builds

---

## 🟢 S

301. **safe-buffer** — Patches the `Buffer` API to prevent uninitialized memory exposure
302. **scheduler** — The priority-based work scheduler used internally by React's reconciler
303. **semver** — Parses, compares, and validates semantic version strings like `^1.2.3`
304. **set-cookie-parser** — Converts raw `Set-Cookie` HTTP header strings into cookie objects
305. **setimmediate** — Provides `setImmediate` scheduling in browser environments that lack it
306. **shebang-command** — Parses the interpreter directive from a `#!/usr/bin/env node` line
307. **shebang-regex** — Detects `#!/...` shebang lines at the start of files via regex
308. **smart-buffer** — Provides read/write cursor tracking over Node.js Buffer for building binary protocols
309. **socks** — Implements SOCKS4, SOCKS4a, and SOCKS5 proxy protocol in Node.js
310. **socks-proxy-agent** — Routes Node.js HTTP/HTTPS requests through a SOCKS proxy server
311. **source-map** — Reads and writes V3 source maps that map minified code back to original source
312. **source-map-js** — Lightweight source map generator used by PostCSS for CSS source maps
313. **space-separated-tokens** — Parses and stringifies space-delimited HTML attribute values (e.g. `class="a b c"`)
314. **streamx** — Hypercore Protocol's streams library with better backpressure and error handling
315. **string-width** — Gets the visual width of a string accounting for fullwidth CJK characters and ANSI codes
316. **string_decoder** — Converts Buffers to strings with proper multi-byte character handling
317. **stringify-entities** — Converts special characters to HTML entity references
318. **strip-ansi** — Strips terminal ANSI color codes to get plain text from styled strings
319. **strip-json-comments** — Strips `//` and `/* */` comments to parse JSON5-style config files
320. **style-to-js** — Transforms `"color: red; font-size: 16px"` CSS strings into `{color: 'red', fontSize: '16px'}` objects
321. **style-to-object** — Converts inline style attributes into JavaScript property objects
322. **supports-color** — Checks if the current terminal supports 16, 256, or 16 million colors

---

## 🟢 T

323. **tailwind-merge** — Resolves conflicts when merging Tailwind class strings (e.g. removes duplicate/conflicting color classes)
324. **tailwindcss** — Generates CSS utility classes from config; provides the entire Tailwind class system used for ViewTube's styling
325. **tapable** — Provides hook types (SyncHook, AsyncHook, etc.) for extensible plugin architectures; used by Webpack/Rolldown
326. **tar-fs** — Packs and unpacks tar archives to/from the file system; used by Puppeteer
327. **tar-stream** — Low-level streaming tar archive reading and writing
328. **teex** — Splits a single async iterable into multiple independent consumers
329. **text-decoder** — Provides the browser `TextDecoder` API for decoding binary data to strings in Node.js
330. **tiny-invariant** — Throws with a message if a condition is false; used in React Router for guard assertions
331. **tinyglobby** — Lightweight file finder for glob patterns; used by Rolldown for file discovery
332. **trim-lines** — Removes leading/trailing whitespace from each line in a string
333. **trough** — Creates middleware-style processing pipelines; used internally by unified/remark
334. **ts-api-utils** — Provides helper functions for inspecting TypeScript AST nodes in ESLint rules
335. **tslib** — Contains shared TypeScript helper functions (`__extends`, `__awaiter`, etc.) to avoid code duplication
336. **type-check** — Validates JavaScript values against type expressions like `"Number | String"`
337. **typed-query-selector** — Gives `querySelector` precise TypeScript return types based on the selector string
338. **typescript** — Transpiles TypeScript to JavaScript and provides full static type checking for the codebase
339. **typescript-eslint** — The official ESLint + TypeScript integration with TypeScript-aware rules and parser

---

## 🟢 U

340. **undici-types** — Type definitions for Node.js's built-in HTTP client; used by TypeScript for `fetch` types
341. **unified** — The engine that connects remark (Markdown) and rehype (HTML) plugins into a processing pipeline
342. **unist-util-is** — Type guard and tester for unified syntax tree nodes
343. **unist-util-position** — Retrieves line/column position data from AST nodes
344. **unist-util-stringify-position** — Formats unist position objects as human-readable strings for error messages
345. **unist-util-visit** — Traverses AST nodes matching a selector, allowing transformation
346. **unist-util-visit-parents** — Like `unist-util-visit` but also provides the parent node chain
347. **update-browserslist-db** — Downloads the latest browser support data from caniuse for Browserslist
348. **uri-js** — Parses, validates, and resolves URIs according to the full RFC 3986 spec; used by AJV
349. **use-sync-external-store** — The official React API for subscribing components to external stores (Redux)
350. **util-deprecate** — Wraps a function to emit a deprecation warning when it is called

---

## 🟢 V

351. **vfile** — Represents text files as objects with a path, contents, and messages; core to the remark/unified pipeline
352. **vfile-message** — Adds warnings/errors with position info to virtual files in unified pipelines
353. **victory-vendor** — Bundles lodash and D3 specifically for the Victory charting library
354. **vite** — Provides an instant dev server with HMR and optimized production builds using Rolldown/esbuild

---

## 🟢 W–Z

355. **web-streams-polyfill** — Provides `ReadableStream`, `WritableStream`, etc. Web Streams API in Node.js environments
356. **webdriver-bidi-protocol** — TypeScript definitions for the WebDriver bidirectional protocol
357. **which** — Locates a command's full executable path, like the Unix `which` command
358. **word-wrap** — Breaks long strings into lines at word boundaries for terminal output formatting
359. **wrap-ansi** — Word-wraps strings that contain ANSI terminal escape codes without breaking color sequences
360. **wrappy** — Wraps callbacks to ensure a function is only called once with proper `this` binding
361. **ws** — A fast, reliable WebSocket client and server implementation for Node.js
362. **y18n** — Provides `__()` style i18n translation strings for yargs CLI output internationalization
363. **yallist** — An efficient doubly-linked list used internally by `lru-cache`
364. **yargs** — Builds interactive CLI tools with commands, options, help text, and validation
365. **yargs-parser** — Parses `process.argv` into a structured object of flags and values for yargs
366. **yauzl** — Reads ZIP archives with low memory overhead; used by extract-zip
367. **yocto-queue** — A minimal linked-list queue used by `p-limit` to manage concurrent promise operations
368. **zod** — Defines schemas that both validate data and infer TypeScript types at compile time
369. **zod-validation-error** — Converts Zod's verbose validation errors into clean, readable messages
370. **zwitch** — Creates switch-style functions that dispatch based on an object's `type` property; used by unified

---

**Total: 370 packages**
