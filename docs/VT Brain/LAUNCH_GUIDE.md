# 🧪 VIEW TUBE: LAUNCH PROTOCOL v2.1

> [!IMPORTANT]
> FOLLOW THESE STEPS TO INITIALIZE THE CREATOR OS ON PORT 5173.

---

## ⚡ QUICK START COMMAND

Copy and paste this into your terminal to jump straight into the production environment:

```bash
cd /Users/cwb/Downloads/viewtube && npm run dev -- --port 5173
```

---

## 🛠️ STEP-BY-STEP MANUAL LAUNCH

### 1. OPEN TERMINAL
Launch your preferred terminal emulator (Terminal, iTerm2, or VS Code Integrated Terminal).

### 2. NAVIGATE TO PROJECT ROOT
```bash
cd /Users/cwb/Downloads/viewtube
```

### 3. INITIALIZE DEV SERVER
Run the following command to start the Neo-Brutalist engine:
```bash
npm run dev -- --port 5173
```

### 4. ACCESS THE HUB
Once the terminal displays `Ready`, Cmd+Click the link below or paste it into your browser:
**LINK: [http://localhost:5173](http://localhost:5173)**

---

## 🛑 TROUBLESHOOTING: PORT CONFLICTS
If the terminal says `Port 5173 is in use`, run this to kill the ghost process:

```bash
lsof -ti:5173 | xargs kill -9
```

---

## 🎨 BRAND CHECK: NEO-BRUTALIST STANDARDS
*   **Borders**: Check if cards have `border-[4px] border-black`.
*   **Typography**: Ensure headers are `font-black uppercase italic`.
*   **Shadows**: Look for the `shadow-[8px_8px_0px_0px_black]` hard offset.

_Generated by Antigravity AI for the ViewTUBE Creator Ecosystem._
