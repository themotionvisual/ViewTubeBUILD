# Scope guard

This migration is visual-system work only. Do not change Studio Hub feature behavior, YouTube data sources, authentication, analytics queries, tool IDs, persistence, or routing while normalizing the Toolbox UI system. Visual migration and behavioral changes ship separately.
