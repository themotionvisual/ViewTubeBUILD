# Quick reference

The Studio Hub component `src/components/ToolboxUIReferenceLibrary.tsx` is the live visual reference. It must consume production primitives instead of defining alternate shell geometry. Use it to visually certify new toolbox/subtoolbox primitives before migrating feature tools.
