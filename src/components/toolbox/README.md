# Toolbox primitives

This directory owns Main Toolbox geometry only. Nested Subtoolbox geometry belongs to `../subtoolbox/`; dashboard Widget geometry belongs to the widget system. Keep these contracts partitioned so a widget CSS change cannot resize Studio Hub toolboxes.
