import React from "react"
import { AudienceStudioWidget } from "./AudienceStudioWidget"

export const DeviceMatrixWidget = (props: any) => (
  <AudienceStudioWidget {...props} initialTab="devices" />
)
