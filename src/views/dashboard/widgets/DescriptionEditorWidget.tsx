import React from "react"
import { MetadataStudioWidget } from "./MetadataStudioWidget"

export const DescriptionEditorWidget = (props: any) => (
  <MetadataStudioWidget {...props} initialTab="description" />
)
