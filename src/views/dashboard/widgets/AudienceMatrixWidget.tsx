import React from "react"
import { AudienceStudioWidget } from "./AudienceStudioWidget"

export const AudienceMatrixWidget = (props: any) => (
  <AudienceStudioWidget {...props} initialTab="geography" />
)
