import React from "react"
import { MetadataStudioWidget } from "./MetadataStudioWidget"

export const TitleRewriterWidget = (props: any) => (
  <MetadataStudioWidget {...props} initialTab="titles" />
)
