/** Shared mobile-editor state. Both layouts mount this reducer. */
import {useCallback,useMemo,useReducer} from 'react';import type {VtE1Clip,VtE1Project,VtE1Transition} from '../../../../shared/vtE1TimelineContract';import {rippleDeleteTimelineClips,slideTimelineClip,slipTimelineClip,splitTimelineClip} from '../../../../shared/vtE1TimelineOperations.js';
export type TrackKind='video'|'audio'|'overlay'|'caption';export interface Track{id:string;name:string;kind:TrackKind;muted?:boolean;locked?:boolean;hidden?:boolean;color?:string}export interface Selection{clipIds:string[];trackId:string|null;transitionId:string|null}const emptySelection:Selection={clipIds:[],trackId:null,transitionId:null};export type Tool='select'|'trim'|'split'|'text'|'audio'|'transitions'|'effects'|'export';
export interface ClipVisualTransform{x:number;y:number;scaleX:number;scaleY:number;rotation:number;opacity:number;cropLeft:number;cropRight:number;cropTop:number;cropBottom:number}
export const DEFAULT_CLIP_VISUAL_TRANSFORM:ClipVisualTransform={x:0,y:0,scaleX:1,scaleY:1,rotation:0,opacity:1,cropLeft:0,cropRight:0,cropTop:0,cropBottom:0};
export const readClipVisualTransform=(clip:VtE1Clip):ClipVisualTransform=>{const raw=(clip as VtE1Clip&{transform?:Partial<ClipVisualTransform>;cropLeft?:number;cropRight?:number;cropTop?:number;cropBottom?:number}).transform??{};const c=clip as VtE1Clip&Partial<ClipVisualTransform>;return {...DEFAULT_CLIP_VISUAL_TRANSFORM,...raw,cropLeft:c.cropLeft??raw.cropLeft??0,cropRight:c.cropRight??raw.cropRight??0,cropTop:c.cropTop??raw.cropTop??0,cropBottom:c.cropBottom??raw.cropBottom??0}};
export interface EditorState{project:VtE1Project&{tracks:Track[];durationSec:number};playheadSec:number;playing:boolean;playbackRate:number;zoomPxPerSec:number;selection:Selection;tool:Tool;panel:{open:boolean;id:Tool;height:number};history:{past:string[];future:string[]}}
export type EditorAction={type:'setPlayhead';sec:number}|{type:'setPlaying';playing:boolean}|{type:'togglePlaying'}|{type:'setPlaybackRate';rate:number}|{type:'setZoom';pxPerSec:number}|{type:'selectClip';id:string;additive?:boolean}|{type:'selectTrack';id:string|null}|{type:'selectTransition';id:string|null}|{type:'clearSelection'}|{type:'setTool';tool:Tool}|{type:'openPanel';id:Tool;height?:number}|{type:'closePanel'}|{type:'setPanelHeight';height:number}|{type:'setPanelId';id:Tool}|{type:'addClip';clip:VtE1Clip}|{type:'updateClip';id:string;patch:Partial<VtE1Clip>}|{type:'updateClipTransform';id:string;patch:Partial<ClipVisualTransform>}|{type:'resetClipTransform';id:string}|{type:'moveClip';id:string;deltaSec:number}|{type:'trimClip';id:string;side:'left'|'right';sec:number}|{type:'splitClipAtPlayhead';id:string}|{type:'slipClip';id:string;deltaSec:number;sourceDurationSec?:number}|{type:'slideClip';id:string;deltaSec:number}|{type:'deleteClips';ids:string[]}|{type:'rippleDeleteClips';ids:string[]}|{type:'duplicateClip';id:string}|{type:'muteTrack';id:string;muted?:boolean}|{type:'lockTrack';id:string;locked?:boolean}|{type:'hideTrack';id:string;hidden?:boolean}|{type:'addTransition';transition:VtE1Transition}|{type:'removeTransition';id:string}|{type:'undo'}|{type:'redo'};
const snapshot=(s:EditorState)=>JSON.stringify({project:s.project,playhead:s.playheadSec,selection:s.selection}),withHistory=(p:EditorState,n:EditorState):EditorState=>({...n,history:{past:[...p.history.past.slice(-49),snapshot(p)],future:[]}}),clampSec=(v:number,max:number)=>Math.max(0,Math.min(v,max));const withoutTransitionsForClips=(t:VtE1Transition[]|undefined,ids:string[])=>{const removed=new Set(ids);return(t??[]).filter(x=>!removed.has(x.leftClipId)&&!removed.has(x.rightClipId))};
export function initialState(project?:Partial<EditorState['project']>):EditorState{const p:EditorState['project']={clips:[],transitions:[],tracks:[{id:'t_video',name:'Video',kind:'video'},{id:'t_overlay',name:'Overlay',kind:'overlay'},{id:'t_audio',name:'Audio',kind:'audio'}],durationSec:30,...(project??{})};return{project:p,playheadSec:0,playing:false,playbackRate:1,zoomPxPerSec:40,selection:emptySelection,tool:'select',panel:{open:false,id:'select',height:.55},history:{past:[],future:[]}}}
export function editorReducer(state:EditorState,action:EditorAction):EditorState{switch(action.type){case'setPlayhead':return{...state,playheadSec:clampSec(action.sec,state.project.durationSec)};case'setPlaying':return{...state,playing:action.playing};case'togglePlaying':return{...state,playing:!state.playing};case'setPlaybackRate':return{...state,playbackRate:Math.max(.1,Math.min(4,action.rate))};case'setZoom':return{...state,zoomPxPerSec:Math.max(4,Math.min(400,action.pxPerSec))};case'selectClip':{const clipIds=action.additive?(state.selection.clipIds.includes(action.id)?state.selection.clipIds.filter(id=>id!==action.id):[...state.selection.clipIds,action.id]):[action.id];return{...state,selection:{...emptySelection,clipIds}}}case'selectTrack':return{...state,selection:{...emptySelection,trackId:action.id}};case'selectTransition':return{...state,selection:{...emptySelection,transitionId:action.id}};case'clearSelection':return{...state,selection:emptySelection};case'setTool':return{...state,tool:action.tool};case'openPanel':return{...state,panel:{open:true,id:action.id,height:action.height??state.panel.height}};case'closePanel':return{...state,panel:{...state.panel,open:false}};case'setPanelHeight':return{...state,panel:{...state.panel,height:Math.max(.15,Math.min(1,action.height))}};case'setPanelId':return{...state,panel:{...state.panel,id:action.id}};case'addClip':return withHistory(state,{...state,project:{...state.project,clips:[...state.project.clips,action.clip]}});case'updateClip':return withHistory(state,{...state,project:{...state.project,clips:state.project.clips.map(c=>c.id===action.id?{...c,...action.patch}:c)}});case'updateClipTransform':{const clips=state.project.clips.map(c=>{if(c.id!==action.id)return c;const current=readClipVisualTransform(c),next={...current,...action.patch};next.opacity=Math.max(0,Math.min(1,next.opacity));next.scaleX=Math.max(.05,next.scaleX);next.scaleY=Math.max(.05,next.scaleY);for(const k of ['cropLeft','cropRight','cropTop','cropBottom'] as const)next[k]=Math.max(0,Math.min(.95,next[k]));if(next.cropLeft+next.cropRight>.98)next.cropRight=.98-next.cropLeft;if(next.cropTop+next.cropBottom>.98)next.cropBottom=.98-next.cropTop;return{...c,transform:{x:next.x,y:next.y,scaleX:next.scaleX,scaleY:next.scaleY,rotation:next.rotation,opacity:next.opacity},cropLeft:next.cropLeft,cropRight:next.cropRight,cropTop:next.cropTop,cropBottom:next.cropBottom} as VtE1Clip});return withHistory(state,{...state,project:{...state.project,clips}})}case'resetClipTransform':return editorReducer(state,{type:'updateClipTransform',id:action.id,patch:DEFAULT_CLIP_VISUAL_TRANSFORM});case'moveClip':{const clips=state.project.clips.map(c=>{if(c.id!==action.id)return c;const d=c.end-c.start,start=Math.max(0,c.start+action.deltaSec);return{...c,start,end:start+d}});return withHistory(state,{...state,project:{...state.project,clips}})}case'trimClip':{const clips=state.project.clips.map(c=>c.id!==action.id?c:action.side==='left'?{...c,start:Math.min(c.end-.1,Math.max(0,action.sec))}:{...c,end:Math.max(c.start+.1,Math.min(state.project.durationSec,action.sec))});return withHistory(state,{...state,project:{...state.project,clips}})}case'splitClipAtPlayhead':{const clip=state.project.clips.find(c=>c.id===action.id);if(!clip)return state;const split=splitTimelineClip(clip,state.playheadSec);if(!split)return state;const right:VtE1Clip={...split.right,id:`${clip.id}_r_${Date.now().toString(36)}`};return withHistory(state,{...state,project:{...state.project,clips:state.project.clips.flatMap(c=>c.id===clip.id?[split.left,right]:[c])}})}case'slipClip':{const clip=state.project.clips.find(c=>c.id===action.id);if(!clip)return state;const slipped=slipTimelineClip(clip,action.deltaSec,action.sourceDurationSec??Infinity);if(!slipped)return state;return withHistory(state,{...state,project:{...state.project,clips:state.project.clips.map(c=>c.id===action.id?slipped:c)}})}case'slideClip':{const r=slideTimelineClip(state.project.clips,action.id,action.deltaSec);return!r.appliedDeltaSec?state:withHistory(state,{...state,project:{...state.project,clips:r.clips}})}case'deleteClips':{const removed=new Set(action.ids);return withHistory(state,{...state,project:{...state.project,clips:state.project.clips.filter(c=>!removed.has(c.id)),transitions:withoutTransitionsForClips(state.project.transitions,action.ids)},selection:emptySelection})}case'rippleDeleteClips':return!action.ids.length?state:withHistory(state,{...state,project:{...state.project,clips:rippleDeleteTimelineClips(state.project.clips,action.ids),transitions:withoutTransitionsForClips(state.project.transitions,action.ids)},selection:emptySelection});case'duplicateClip':{const c=state.project.clips.find(x=>x.id===action.id);if(!c)return state;const d=c.end-c.start,copy:VtE1Clip={...c,id:`${c.id}_dup_${Date.now().toString(36)}`,start:c.end,end:c.end+d};return withHistory(state,{...state,project:{...state.project,clips:[...state.project.clips,copy]}})}case'muteTrack':return{...state,project:{...state.project,tracks:state.project.tracks.map(t=>t.id===action.id?{...t,muted:action.muted??!t.muted}:t)}};case'lockTrack':return{...state,project:{...state.project,tracks:state.project.tracks.map(t=>t.id===action.id?{...t,locked:action.locked??!t.locked}:t)}};case'hideTrack':return{...state,project:{...state.project,tracks:state.project.tracks.map(t=>t.id===action.id?{...t,hidden:action.hidden??!t.hidden}:t)}};case'addTransition':return withHistory(state,{...state,project:{...state.project,transitions:[...(state.project.transitions??[]),action.transition]}});case'removeTransition':return withHistory(state,{...state,project:{...state.project,transitions:(state.project.transitions??[]).filter(t=>(t as{id?:string}).id!==action.id)}});case'undo':{const last=state.history.past.at(-1);if(!last)return state;const p=JSON.parse(last);return{...state,project:p.project,playheadSec:p.playhead,selection:p.selection,history:{past:state.history.past.slice(0,-1),future:[snapshot(state),...state.history.future]}}}case'redo':{const next=state.history.future[0];if(!next)return state;const p=JSON.parse(next);return{...state,project:p.project,playheadSec:p.playhead,selection:p.selection,history:{past:[...state.history.past,snapshot(state)],future:state.history.future.slice(1)}}}default:return state}}
export interface EditorStore{state:EditorState;dispatch:React.Dispatch<EditorAction>;selectedClips:VtE1Clip[];trackById:(id:string)=>Track|undefined;clipsOnTrack:(trackId:string)=>VtE1Clip[];activeClipAtPlayhead:(trackId?:string)=>VtE1Clip|undefined;canUndo:boolean;canRedo:boolean}
export function useEditorState(seed?:Partial<EditorState['project']>):EditorStore{const[state,dispatch]=useReducer(editorReducer,undefined,()=>initialState(seed)),selectedClips=useMemo(()=>state.project.clips.filter(c=>state.selection.clipIds.includes(c.id)),[state.project.clips,state.selection.clipIds]),trackById=useCallback((id:string)=>state.project.tracks.find(t=>t.id===id),[state.project.tracks]),clipsOnTrack=useCallback((id:string)=>state.project.clips.filter(c=>c.trackId===id).sort((a,b)=>a.start-b.start),[state.project.clips]),activeClipAtPlayhead=useCallback((id?:string)=>state.project.clips.find(c=>state.playheadSec>=c.start&&state.playheadSec<c.end&&(!id||c.trackId===id)),[state.project.clips,state.playheadSec]);return{state,dispatch,selectedClips,trackById,clipsOnTrack,activeClipAtPlayhead,canUndo:state.history.past.length>0,canRedo:state.history.future.length>0}}
