/** Fluid mobile preview with direct contextual actions. */
import React,{useRef} from 'react';import {EditorStore} from '../state/editorState';import {useDragScrub,usePinchZoom} from '../hooks/gestures';
export interface PreviewPaneProps{store:EditorStore;renderPreview?:(info:{widthPx:number;heightPx:number})=>React.ReactNode;aspect?:number;className?:string;showScrubHint?:boolean;}
const CYAN='#36E0F6';
export const PreviewPane:React.FC<PreviewPaneProps>=({store,renderPreview,aspect=16/9,className,showScrubHint=true})=>{const {state,dispatch}=store,surfaceRef=useRef<HTMLDivElement>(null),selected=store.selectedClips[0];const scrub=useDragScrub({pixelsPerUnit:6,cancelIfVertical:24,onScrubStart:()=>dispatch({type:'setPlaying',playing:false}),onScrub:d=>dispatch({type:'setPlayhead',sec:state.playheadSec+(d/30)*.02})}),{pinch,handlers}=usePinchZoom({onPinch:()=>{}}),tapStart=useRef<{x:number;y:number;t:number}|null>(null);const small:React.CSSProperties={height:24,minWidth:28,border:'2px solid #000',borderRadius:4,background:'#fff',fontSize:8,fontWeight:900,padding:'0 5px'};return <div className={className} data-aspect={aspect} style={{position:'relative',width:'100%',height:'100%',minWidth:0,minHeight:0,background:'#000',overflow:'hidden',borderRadius:6}}><div ref={surfaceRef} onPointerDown={e=>{tapStart.current={x:e.clientX,y:e.clientY,t:e.timeStamp};handlers.onPointerDown(e);scrub.onPointerDown(e)}} onPointerMove={e=>{handlers.onPointerMove(e);scrub.onPointerMove(e)}} onPointerUp={e=>{handlers.onPointerUp(e);scrub.onPointerUp(e);if(tapStart.current&&Math.abs(e.clientX-tapStart.current.x)<8&&Math.abs(e.clientY-tapStart.current.y)<8&&e.timeStamp-tapStart.current.t<250)dispatch({type:'togglePlaying'});tapStart.current=null}} onPointerCancel={e=>{handlers.onPointerCancel(e);scrub.onPointerCancel(e);tapStart.current=null}} style={{position:'absolute',inset:0,background:'#111',touchAction:'none',userSelect:'none',transform:pinch.active?`scale(${pinch.scale})`:undefined,transformOrigin:'center',transition:pinch.active?'none':'transform 200ms ease',display:'grid',placeItems:'center'}}>{renderPreview?renderPreview({widthPx:0,heightPx:0}):<FallbackPreview state={state}/>}</div><div style={{position:'absolute',top:5,left:6,padding:'2px 5px',borderRadius:4,background:'rgba(0,0,0,.6)',color:'#fff',fontSize:8,fontWeight:800,pointerEvents:'none'}}>{formatTime(state.playheadSec)} / {formatTime(state.project.durationSec)}</div>{selected&&<div style={{position:'absolute',top:5,right:5,display:'flex',gap:3}}><button style={{...small,background:CYAN}} onClick={e=>{e.stopPropagation();dispatch({type:'splitClipAtPlayhead',id:selected.id})}}>SPLIT</button><button style={small} onClick={e=>{e.stopPropagation();dispatch({type:'duplicateClip',id:selected.id})}}>DUP</button><button style={{...small,background:'#FA618A'}} onClick={e=>{e.stopPropagation();dispatch({type:'deleteClips',ids:[selected.id]})}}>DEL</button></div>}{!state.playing&&<div style={{position:'absolute',width:42,height:42,borderRadius:21,background:'rgba(0,0,0,.5)',display:'grid',placeItems:'center',color:'#fff',pointerEvents:'none',opacity:.75}}>▶</div>}{showScrubHint&&state.playheadSec===0&&!state.playing&&<div style={{position:'absolute',bottom:5,left:'50%',transform:'translateX(-50%)',padding:'2px 6px',borderRadius:999,background:'rgba(0,0,0,.55)',color:'#fff',fontSize:8,fontWeight:700,pointerEvents:'none',whiteSpace:'nowrap'}}>Tap · scrub · pinch</div>}</div>};
function formatTime(sec:number){const s=Math.max(0,sec);return `${Math.floor(s/60).toString().padStart(2,'0')}:${Math.floor(s%60).toString().padStart(2,'0')}.${Math.floor((s*100)%100).toString().padStart(2,'0')}`}
const FallbackPreview:React.FC<{state:EditorStore['state']}>=({state})=>{const clip=state.project.clips.find(c=>state.playheadSec>=c.start&&state.playheadSec<c.end);return <div style={{position:'absolute',inset:0,display:'grid',placeItems:'center',color:'#e2e8f0'}}><div style={{textAlign:'center',opacity:.6}}><div style={{fontSize:9,letterSpacing:1,textTransform:'uppercase'}}>Preview</div><div style={{fontSize:16,fontWeight:700,marginTop:3}}>{clip?String(clip.id).slice(0,20):'No clip'}</div></div></div>};
