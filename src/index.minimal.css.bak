@tailwind base;
@tailwind components;
@tailwind utilities;

/* Minimal baseline to unblock dev server */
html, body, #root { height: 100%; }
body { margin: 0; font-family: system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial; }
